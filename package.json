{
  "name": "lawnmind",
  "version": "0.1.0",
  "private": true,
  "description": "LawnMind — AI-powered lawn care companion (Expo app + Fastify API)",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix app install",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix app run web",
    "dev:app": "npm --prefix app run start",
    "test": "npm --prefix server test",
    "typecheck": "npm --prefix server run typecheck && npm --prefix app run typecheck",
    "build:web": "npm --prefix app exec expo export --platform web"
  }
}
