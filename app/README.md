# LawnMind app (Expo)

See the [root README](../README.md) for setup. Runs on iOS, Android, and web:

```bash
npm run web   # browser at http://localhost:8081
npm start     # Expo Go / simulators
```
